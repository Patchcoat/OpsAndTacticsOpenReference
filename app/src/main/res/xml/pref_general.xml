<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bookmark Collection. -->
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_bookmark_collection"
        android:entryValues="@array/pref_bookmark_collection_values"
        android:key="bookmark_collection"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_pref_bookmark_collection" />

</PreferenceScreen>
